TIME_ZONE=TZ from https://en.wikipedia.org/wiki/List_of_tz_database_time_zones#List here

USER1=name here
USER1_CONFIG=/path/to/config/data/here/
USER1_PORT=desired port number here

# the container specs below allow you to spin up additional heimdall dashboards 
# such as for other users, a version with tailscale IPs for external access, etc. 
# if these aren't needed, delete or comment out unneeded yaml below this point
# you can also copy as needed to add more instances of heimdall

USER2=name here
USER2_CONFIG=/path/to/config/data/here/
USER2_PORT=desired port number here

USER3=name here
USER3_CONFIG=/path/to/config/data/here/
USER3_PORT=desired port number here